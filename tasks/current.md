# Current Tasks

Активная работа. До 5 задач одновременно. Закрытые удаляются (история — в git).

| status | task | skill / workflow | дата открытия |
|--------|------|------------------|---------------|
| done   | Инициализация AI-инфраструктуры (CLAUDE.md, skills, docs, prompts, tasks) | — | 2026-05-11 |
| done   | Миграция UI на Hugeicons: создан `Icon.jsx`-реестр, заменены все эмодзи в UI и `src/data/*`, обновлены CLAUDE.md / architecture.md / content-rules.md / 2 skills | `react-knowledge-ui` | 2026-05-11 |
| done   | Fullscreen preview промптов: `PromptModal.jsx` + кнопка expand в `PromptCard`, иконка `expand` (Maximize01) добавлена в реестр, стили модалки в `App.css` (Esc / click-outside / mobile-full) | `react-knowledge-ui` | 2026-05-11 |
| done   | Библиотека шаблонов: новый датасет `src/data/promptLibrary.js` (9 категорий, 3 уровня, 42 шаблона), `PromptLibraryModal.jsx` (sidebar + поиск + фильтр уровня + grid), кнопка «Библиотека» в Toolbar, переиспользует `PromptModal` для fullscreen-чтения, обновлены docs/architecture.md и docs/knowledge-structure.md | `react-knowledge-ui` + `content-structurer` | 2026-05-11 |
| done   | P1 фундамент по итогам аудита: lint:data (npm script), URL routing (useHashRoute), Cmd+K (CommandPalette), Bookmarks (useBookmarks + UI), Try in Claude (deep-link в PromptModal), «Что дальше» в финале tutorial (library + node). 1 script, 2 hooks, 1 компонент, +12 KB JS | `react-knowledge-ui` | 2026-05-11 |
| done   | P2 структурные ходы (3/5): node progress (useNodeProgress + 2 кнопки + маркер на узле), cross-links (relatedIds в схеме + двусторонний индекс + блок «Связано»), learning paths (5 кураторских маршрутов + вкладка в CoursesModal). Линт расширен на 2 проверки. №10 (CI) и №11 (AI Companion) отложены. +9 KB JS | `react-knowledge-ui` + `content-structurer` | 2026-05-11 |
| done   | Контент — обезличивание под любую индустрию: убраны FDS / Finnish Design Shop, личное био Product Owner, «кофейня», конкретные «senior копирайтер / редактор / разработчик / UX-аудитор», «10 лет опыта». UX-аудит → экспертный аудит, дизайн-система → аудит консистентности. Linear/Jira → «ваш трекер задач». ~40+ замен в mindmapData + prompts + promptLibrary + tutorials. Все ссылки/айди обновлены, lint зелёный | `content-structurer` | 2026-05-11 |
| done   | Терминал — 2 новых раздела (Работа в терминале + Настройка) с 9 листьями. cc-slash расширен 9 командами (/init /clear /compact /model /permissions /agents /mcp /review + custom). +20 узлов (64 → 84). UI-фикс: заголовки на узлах больше не обрезаются (white-space: normal + word-break, max-width 300px, LEAF_HEIGHT 78→92) | `mindmap-expander` + `react-knowledge-ui` | 2026-05-11 |
| done   | Ребрендинг → Claude Atlas (Toolbar / index.html / root node / README / docs). Технические идентификаторы (localStorage keys, package.json) сохранены | `react-knowledge-ui` | 2026-05-11 |
| done   | UX-перетряс: floating Zoom (низ-право canvas) + floating Filter chips (верх-лево canvas) на desktop. Mobile: Toolbar и PromptsSection скрыты, 4 FAB в углах (TL=меню, TR=Обучение, BL=Промпты, BR=Библиотека). Базовый BottomSheet (Esc/click-outside/swipe). DetailPanel рендерится через BottomSheet на mobile (useIsMobile hook), aside на desktop. Prev/Next навигация (DetailNavFooter) + хоткеи ←/→ — DFS-обход с учётом фильтра. +5 компонентов, +1 hook | `react-knowledge-ui` | 2026-05-11 |
| done   | Связь Cmd+K / paths / tutorials с mindmap: (a) `findAncestorPath` авто-раскрывает всех предков выбранного узла — глубокие deep-links теперь визуально позиционируются на mindmap; (b) `selected` включает tutorial route — карта позиционируется на узле под открытой модалкой; (c) закрытие tutorial оставляет узел открытым в DetailPanel; (d) canvas получает subtle category-тинт (7% подмешка цвета категории + цветная рамка) когда активен фильтр ИЛИ выбран узел — `--canvas-tint` CSS-переменная + `.is-tinted` класс. Читаемость сохранена | `react-knowledge-ui` | 2026-05-11 |
| done   | Floating DetailPanel + Prompts FAB: (a) DetailPanel на desktop — плавающая карточка с 40px инсетами и закруглением 16px (вместо прилепленной к правому краю); (b) убран empty-state «Откройте узел» — он избыточен; (c) Zoom-controls автоматически сдвигаются влево когда панель открыта (`.has-detail` класс); (d) Готовые промпты убраны из нижней секции страницы — теперь доступны через FAB в bottom-left на desktop + mobile (единый UX); (e) `PromptsSection` снят с рендера | `react-knowledge-ui` | 2026-05-11 |
| done   | Header: убран подзаголовок «карта знаний, обучение и шаблоны» (бренд однострочный). Поиск спрятан за кнопку-иконку: клик → inline input разворачивается с автофокусом, Esc / blur при пустом query → сворачивается. Активный фильтр-запрос показан индикатором (синяя рамка + оранжевая точка) | `react-knowledge-ui` | 2026-05-11 |
| done   | Root-узел Claude Atlas — фикс читаемости при фильтре: при `.is-matched` сохраняется тёмный фон + белый текст. Подсветка идёт через цветную рамку и glow по `--canvas-tint` (цвет активной категории), а не через светлый `accent-soft` фон | `react-knowledge-ui` | 2026-05-11 |
| done   | Учебная система — расширение: (1) +8 новых полей в каждом из 11 туториалов: `level`, `whatItIs`, `prerequisites`, `outcomes`, `approach`, `applyIn`, `relatedPrompts`, `pitfalls`, `exercises`. (2) Новый компонент `TutorialDetail.jsx` — превью-карточка обучения; клик по курсу в CoursesModal открывает split-view (на mobile — замещение). 9 контентных секций + CTA «Начать / К узлу». (3) Quick / Standard / Deep mode toggle в `TutorialModal` (localStorage), Quick прячет why/tip/troubleshoot/example для ускорения. (4) Exercises блок на последнем шаге. (5) Lint расширен на 5 новых валидаций. +2 компонента | `content-structurer` + `react-knowledge-ui` | 2026-05-11 |
| done   | UI: 1) `CanvasHeader` (плавающий top-left) — логотип Claude Atlas + chevron-dropdown (Библиотека / Обучение) + поиск-иконка с автофокусом при раскрытии. 2) Чипсы фильтра переехали top-center. 3) Zoom переехал bottom-center с pill-формой и круглыми углами; включил «Раскрыть всё / Свернуть всё» слева от zoom-функций. 4) Toolbar полностью удалён из App.jsx | `react-knowledge-ui` | 2026-05-11 |
| done   | Контент-расширение (4 фазы, 17 узлов + 3 tutorial): Фаза 1 — `b-claude` (Что такое Claude), `b-anthropic` + новая ветка `platform` (Платформа и доступ) с 5 узлами: тарифы / лимиты / модели-по-плану / платформы / rate-limits. Фаза 2 — новая ветка `capabilities` (Возможности) с 8 узлами: vision / files / web search / memory / computer use / tool use / prompt caching / citations. Фаза 3 — 3 новых tutorial: intro (Знакомство), platform (Выбор тарифа), capabilities (Изображения и файлы). Фаза 4 — `docs/learning-design-rules.md` (10 правил AI-педагогики), `docs/maintenance.md` (deprecate-watch и каденс) + 2 новых skill (`ai-pedagogy-architect`, `content-gap-auditor`). Mindmap: 84 → 101 узел. Tutorials: 11 → 14 | `ai-pedagogy-architect` + `content-gap-auditor` | 2026-05-11 |
| done   | Cleanup + audit + news-watcher: (1) удалены мёртвые `Toolbar.jsx` и `PromptsSection.jsx`. (2) `ready-prompts` перенесён из root под `prompting` — root.width 13 → 12, варнинг lint ушёл. (3) Disambiguation duplicate-title: `p-instructions` → «Project Instructions», `i-claudemd` → «CLAUDE.md (как файл)». (4) Новый узел `s-vs-subagents` («Skill ≠ Sub-agent») — разъясняющий узел самой частой путаницы. (5) Skill `news-watcher` (`skills/news-watcher/SKILL.md`) — 6-шаговый workflow для еженедельной проверки релизов Anthropic с источниками и форматом отчёта. backlog обновлён | `content-gap-auditor` | 2026-05-12 |
| done   | Backup проекта (zip 263 KB, без node_modules/dist/.git) + Level Filter — глобальный фильтр уровня (Новичок/Продвинутый/Эксперт). Тегированы 12 root-веток полем `minLevel` (Beginner: basics, platform, projects, artifacts, instructions; Intermediate: capabilities, prompting, scenarios, skills; Advanced: claude-code, mcp, agents). Новый `useLevelFilter` hook (localStorage), новый `LevelToggle.jsx` (pill с popover). При смене уровня — авто-пересборка expandedIds (soft mode, можно вручную развернуть). Контейнер `.canvas-top-center` объединяет LevelToggle + CanvasFilters. Default = Эксперт (backward compat). На mobile скрыт (через @media) | `react-knowledge-ui` | 2026-05-12 |
| done   | Контентный спринт + Профиль (Phase 1+2). КОНТЕНТ (+4 узла): `pl-privacy` (Конфиденциальность данных) в platform, `b-help` (Где найти помощь) в basics, `m-patterns` (MCP паттерны проектирования) и `m-debug` (Отладка MCP) в MCP. 102 → 106 узлов. ПРОФИЛЬ: `ProfileFab.jsx` (TR-кнопка с user-иконкой), `ProfilePanel.jsx` (содержимое — header «Аноним» + LEVEL switcher + streak/totalDays + tutorial-progress bar + node-stats grid + achievements + export/import .json + reset all + Pro-teaser «Скоро»). Новый `useActivityLog` hook (отслеживает streak по дням использования, max 365 дней). На desktop — dropdown справа сверху, на mobile — BottomSheet. 2 новые иконки: `user`, `star` | `react-knowledge-ui` + `content-gap-auditor` | 2026-05-12 |
| done   | Аудит-спринт A (контент + обучение). АУДИТ: проход по 106 узлам — все имеют полный 6-вопросный details, lint чист, глубина ≤ 3, ширина ≤ 9. Найдено 12 контентных пробелов + 6 missing cross-links + 5 узлов без tutorial-покрытия. КОНТЕНТ (+4 узла): `pr-prefill` (Prefill — начать ответ), `pr-structured` (Структурированный вывод / JSON), `pr-iterate` (Итерация промпта) в prompting; `pl-compare` (Claude.ai vs Code vs API) в platform. 106 → 110 узлов. CROSS-LINKS: cap-search↔b-knowledge, cap-memory→projects+cap-tools, m-security↔cc-cfg-permissions, pl-rate↔cap-caching, b-context→cc-cmd-compact+clear, cap-tools→cc-subagents. ОБУЧЕНИЕ (+2 tutorial): `prompting-techniques` (7-шаговый hands-on: CoT, Few-shot, XML, Prefill, Structured, итерация) и `subagents` (6-шаговый hands-on: Explore, Plan, custom subagent, Skill vs Subagent). 14 → 16 туториалов. Build clean | `content-gap-auditor` + `ai-pedagogy-architect` | 2026-05-12 |
| done   | Аудит-спринт B (P2 контент + обучение). КОНТЕНТ (+6 узлов): `cap-code-exec` (Code execution — Python sandbox) в capabilities; `cc-plan-mode` (Plan Mode — план до правок), `cc-ide` (VS Code / JetBrains расширения) в claude-code; `ag-principles` (Принципы дизайна агента: 6-пунктовый чек-лист) в agents; `sc-data` (Анализ данных CSV/XLSX) в scenarios; `b-prompt-injection` (Prompt injection — defensive awareness) в basics. 110 → 116 узлов. ОБУЧЕНИЕ (+2 tutorial): `tool-use` (6-шаговый: схема потока, JSON Schema, description, узкий набор tools, tool use vs MCP) и `hooks` (6-шаговый: Pre/Post ToolUse, автоформат, security guard, Stop-логи, выбор hook vs slash vs subagent). 16 → 18 туториалов. Lint clean, build 511 KB | `content-gap-auditor` + `ai-pedagogy-architect` | 2026-05-12 |
| done   | i18n Phase 1 — Foundation (EN default + RU source + FI target). Архитектура: `src/i18n/{config,strings,t,LocaleContext}.{js,jsx}` + `src/locales/{en,ru,fi}/ui.json` (40+ ключей для ProfilePanel). Resolve локали: URL hash `#/<lang>/...` → localStorage `claude-mindmap:locale:v1` → `navigator.language` → default `en`. Sync: `<html lang>` обновляется при смене локали. `useHashRoute` обновлён парсить locale-prefix; setRoute сохраняет prefix. `useLocale()` + `useT()` хуки через `LocaleContext`. t() с fallback на RU и progressive split для полу-плоских JSON ключей (`profile.data.export`). LocaleSwitcher в ProfilePanel — 3 кнопки EN/RU/FI с локализованными labels. ProfilePanel полностью переведён (40+ строк × 3 локали). Build 518 KB (+7 KB), lint clean. ВИЗУАЛЬНО проверено: переключение языка работает, deep-link `#/ru/node/pr-prefill` парсится, нет console errors | `react-knowledge-ui` + i18n-инфра | 2026-05-12 |
| done   | i18n Phase 2 — UI strings extraction. Все ~180+ UI-строк извлечены из 13 компонентов (CanvasHeader, CanvasFilters, CanvasZoom, DetailPanel, DetailNavFooter, TutorialModal, TutorialDetail, CoursesModal, PromptLibraryModal, PromptModal, CommandPalette, MobileFab, BottomSheet, Tooltip, MindmapNode, ProfileFab) в `locales/{en,ru,fi}/ui.json`. 17 namespace-блоков: common, level, category, header, zoom, node, nav, detail, tutorial, tutorialDetail, courses, library, prompt, palette, mobile, tooltip, profile. CATEGORIES labels локализуются через `t('category.<id>')` (id остаётся русским — рефактор в Phase 3). PROMPT_LEVELS / PROMPT_CATEGORIES — пока RU из data-файлов (Phase 3). Build 540 KB (+22 KB), lint clean, zero console warnings. ВИЗУАЛЬНО проверено EN (chips "All/Basics/Configuration/..." + zoom "Expand all/Collapse all/Fit/Reset") и FI (chips "Kaikki/Perusasiat/..." + zoom "Laajenna/Sulje/Sovita/Nollaa" + DetailPanel "MIKÄ TÄMÄ ON/MIKSI.../MILLOIN.../VAIKUTUS.../ESIMERKKI.../YLEISET VIRHEET" + TutorialModal hotkeys "siirtyminen/valmis ja seuraava/sulje") | `react-knowledge-ui` + i18n-инфра | 2026-05-12 |
| done   | i18n Phase 3a — Nodes content split (116 узлов × 7 полей = ~800 строк). Скрипты: `scripts/extract-node-content.mjs` извлекает title+details в `locales/ru/nodes.json` (123 KB); `scripts/strip-node-content.mjs` перегенерирует `mindmapData.js` со структурой-only (id, icon, category, children, relatedIds, minLevel, isRoot). 1262 → 823 строки. `locales/{en,fi}/nodes.json` — копии RU (Phase 5 переведёт через AI). Резолвер: `src/i18n/useNode.js` — `useNodeContent(node)` + `getNodeContent(t, id)`. `strings.js` перепакован — UI на верхнем уровне локального bag, `nodes` namespace отдельно. `t.js` упрощён: `lookup` ходит по плоскому bag через progressive-split. КОМПОНЕНТЫ: MindmapNode (title → t()), DetailPanel (title+details+children+related через getNodeContent), DetailNavFooter (prev/next title через t()), App.jsx (searchTree принимает searchableById, пересобирается при смене локали), CommandPalette (flattenNodes принимает t-функцию, индекс пересобирается при locale), TutorialModal (nextSuggestions через t()), CoursesModal (stepTitle принимает t). LINT расширен: schema-проверки 6-полей идут по `locales/ru/nodes.json`, + completeness checks для en/fi. Build 687 KB (+147 KB — 3× nodes.json bundle). ВИЗУАЛЬНО: zero errors, 93 видимых узла рендерят корректные titles (RU), переключение EN/RU/FI работает, deep-links работают | `react-knowledge-ui` + i18n-инфра | 2026-05-12 |
| done   | i18n Phase 3b — Tutorials content split (18 туториалов с полями title/subtitle/whatItIs/approach/outcomes[]/applyIn[]/pitfalls[]/exercises[]/totalTime + steps[] с title/time/why/instructions[]/prompt/example/validate/tip/troubleshoot). Скрипты: `extract-tutorial-content.mjs` → `locales/ru/tutorials.json` (195 KB); `strip-tutorial-content.mjs` → `tutorials.js` со структурой (nodeId, icon, level, prerequisites, relatedPrompts, next, steps:[{id}]). `locales/{en,fi}/tutorials.json` — копии RU. Резолвер: `src/i18n/useTutorial.js` — `useTutorialContent(id)`, `getLocalizedTutorial(id, locale)`. Резолвер сливает структурный объект с контентом из локали, сохраняет совместимый shape: компоненты продолжают читать `tut.title`, `tut.steps[i].instructions` без правок логики. КОМПОНЕНТЫ: TutorialModal (использует useTutorialContent), TutorialDetail (теперь принимает `tutId` вместо `tut`, локализует внутри), CoursesModal (items list через getLocalizedTutorial, selectedTutorialKey — id из tutorials map), CommandPalette (tuts index через getLocalizedTutorial), DetailPanel (tutorial.totalTime через getLocalizedTutorial). LINT расширен: проверки контента tutorials идут по `locales/ru/tutorials.json` + completeness checks для en/fi. Build 911 KB (+224 KB — 3× tutorials.json по 195 KB). ВИЗУАЛЬНО: TutorialModal рендерит "Освоить основы Claude" в EN UI ("Tutorial · 15 мин" kicker, "← → navigate / ⌘+↵ done and next / Esc close" hotkeys, "Step completed" checkbox, "Quick/Standard/Deep" toggle); FI: "Oppitunti · 20 мин" + "siirtyminen/valmis ja seuraava/sulje" hotkeys. Контент туториалов остаётся RU до Phase 5 (AI-translate) | `react-knowledge-ui` + i18n-инфра | 2026-05-12 |
| done   | i18n Phase 3c — Prompts + Library content split (10 featured + 42 templates + 9 categories + 3 levels). Скрипты: `extract-prompts-content.mjs` → `locales/ru/prompts.json` (13 KB) + `locales/ru/prompt-library.json` (36 KB); `strip-prompts-content.mjs` → `prompts.js` структура {id, icon}, `promptLibrary.js` структура {id, category, level, icon} + PROMPT_CATEGORIES{key:{icon}} + PROMPT_LEVELS{key:{color}}. `locales/{en,fi}/{prompts,prompt-library}.json` — копии RU. Резолвер: `src/i18n/usePrompt.js` — `getLocalizedFeaturedPrompt`, `getLocalizedLibraryTemplate`, `getAllLocalizedLibrary`, `getLibraryCategory`, `getAllLibraryCategories`, `getLibraryLevel`, `getAllLibraryLevels`. КОМПОНЕНТЫ: MobileFab (resolveFeaturedPrompt в loop по `readyPrompts`), PromptLibraryModal (`localizedLibrary`/`localizedCategories`/`localizedLevels` через useMemo по locale, filtered теперь по локализованной коллекции), CommandPalette (prompts index через getLocalizedLibraryTemplate), TutorialDetail (relatedPrompts через локализаторы), CoursesModal/PathsList (stepTitle принимает locale, ищет prompt через getLocalizedLibraryTemplate + getLocalizedFeaturedPrompt). LINT: проверки content prompts/library через locales/ru, completeness checks для en/fi, отдельно для categories/levels labels. Build 973 KB (+62 KB — 3× ~50 KB prompts files). ВИЗУАЛЬНО: PromptLibraryModal в EN — "Prompt library", "42 templates · 9 categories", levels "All/Beginner/Intermediate/Expert"; FI — "Prompt-kirjasto", "42 mallia · 9 kategoriaa", "Kaikki/Aloittelija/Edistynyt/Asiantuntija". Контент темплейтов и категорий остаётся RU до Phase 5 | `react-knowledge-ui` + i18n-инфра | 2026-05-12 |
| done   | i18n Phase 3d — Learning paths content split (5 paths × {title, description, duration, steps[].why}). Скрипт `extract-paths-content.mjs` → `locales/ru/paths.json` (5 KB) с композитными ключами `<type>:<id>` для шагов. `strip-paths-content.mjs` → `learningPaths.js` структура {id, icon, level, steps:[{type, id}]}. `locales/{en,fi}/paths.json` — копии RU. Резолвер: `src/i18n/usePath.js` — `getLocalizedPath(id, locale)` + `getAllLocalizedPaths(locale)`, сохраняет shape для совместимости (title/description/duration/steps[].why). КОМПОНЕНТЫ: CoursesModal/PathsList — пас `paths={getAllLocalizedPaths(locale)}` вместо `learningPaths`. LINT: проверки контента paths через locales/ru/paths.json + completeness checks для en/fi. Build 977 KB. ВИЗУАЛЬНО: CoursesModal в EN — "Claude tutorials / Paths 5 / Courses 18", в FI — "Claude-oppitunnit / Polut 5 / Kurssit 18". Path titles и step.why остаются RU до Phase 5 | `react-knowledge-ui` + i18n-инфра | 2026-05-12 |
| done   | i18n Phase 4 — Glossary + translate-skill. `src/locales/glossary.json` — 126 английских терминов (брендовая лексика Claude/Anthropic/Opus/Sonnet/Haiku, продуктовая Projects/Artifacts/Skills/MCP/CLAUDE.md, фичи Prompt injection/Tool use/Few-shot/Chain of Thought/Prefill, файлы settings.json/SKILL.md, dev-инструменты GitHub/Linear/Figma/Notion, tech-аббревиатуры API/SDK/CLI/IDE/JSON/XML/HTML). `skills/translate-to-finnish/SKILL.md` — полная спецификация workflow: 7 шагов (load glossary → load source → batch → translate → validate → save → report), 10 правил перевода (включая финские падежные окончания на английских терминах через `:` и `-`), валидация (JSON-syntax/keys-match/glossary-preserved/no-russian-leftover/placeholders-match), 6 антипаттернов. LINT расширен: glossary-check сравнивает counts терминов в RU vs FI; если в FI термин встречается <50% от RU — warning. Сейчас FI = копия RU, counts совпадают, glossary-check проходит без warnings | `automation-architect` + i18n-инфра | 2026-05-12 |

## Заметки
- Перед началом задачи: переведи `status` в `in_progress` и обнови дату.
- После завершения: удали из этого файла. История — в коммитах.
- Если задача забуксовала > 2 дней — переноси в `backlog.md` или открой обсуждение.
