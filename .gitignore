node_modules
dist
.DS_Store
*.log
.vite
.claude/
*.zip
.env
.env.local
.env.*.local

# Playwright
test-results/
playwright-report/

# Supabase CLI local temp/state
supabase/.temp/
supabase/.branches/

# локальное демо лоадеров — НЕ деплоить
loaders-demo.html

# локальное демо CTA — НЕ деплоить
cta-demo.html

# локальные демо-лендинги — НЕ деплоить (работаем отдельно от сервера)
landing-demo.html
atlas-demo/

# резервные копии лендинга — НЕ в git
atlas-demo-backups/
